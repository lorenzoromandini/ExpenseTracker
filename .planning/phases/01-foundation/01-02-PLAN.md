---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/navigation/index.tsx
  - src/navigation/AuthNavigator.tsx
  - src/navigation/AppNavigator.tsx
  - src/navigation/types.ts
  - src/screens/WelcomeScreen.tsx
  - src/screens/LoginScreen.tsx
  - src/screens/RegisterScreen.tsx
  - src/screens/HomeScreen.tsx
  - App.tsx
autonomous: true

must_haves:
  truths:
    - "User sees welcome screen on first launch"
    - "User can navigate between auth screens (login, register)"
    - "Navigation structure supports future main app screens"
  artifacts:
    - path: "src/navigation/index.tsx"
      provides: "Root navigation container"
      exports: ["Navigation"]
    - path: "src/navigation/AuthNavigator.tsx"
      provides: "Authentication flow navigation"
      contains: "createNativeStackNavigator"
    - path: "src/navigation/AppNavigator.tsx"
      provides: "Main app navigation (tabs)"
      contains: "createBottomTabNavigator"
    - path: "src/screens/WelcomeScreen.tsx"
      provides: "Initial welcome/onboarding entry point"
      min_lines: 30
    - path: "src/screens/LoginScreen.tsx"
      provides: "Login form screen"
      min_lines: 40
    - path: "src/screens/RegisterScreen.tsx"
      provides: "Registration form screen"
      min_lines: 40
    - path: "src/screens/HomeScreen.tsx"
      provides: "Main app home screen"
      min_lines: 20
  key_links:
    - from: "App.tsx"
      to: "src/navigation/index.tsx"
      via: "import Navigation"
      pattern: "import.*navigation"
    - from: "src/navigation/index.tsx"
      to: "AuthNavigator|AppNavigator"
      via: "conditional rendering based on auth state"
      pattern: "isAuthenticated.*AuthNavigator.*AppNavigator"
    - from: "src/navigation/AuthNavigator.tsx"
      to: "WelcomeScreen|LoginScreen|RegisterScreen"
      via: "Stack.Screen components"
      pattern: "Stack.Screen.*name.*Welcome"
---

<objective>
Implement React Navigation 7 structure with auth flow (welcome, login, register) and main app navigator.

Purpose: Enable user navigation between auth screens and main app; supports account creation (AUTH-01) and authentication (AUTH-02).
Output: Working navigation with screen stubs that can be wired to real logic in subsequent plans.
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Dependencies: Plan 01-01 must be complete (Expo project structure exists)
# Requirements covered: AUTH-01 (account creation), AUTH-02 (login), AUTH-03 (password reset), AUTH-04 (cloud sync), AUTH-05 (onboarding)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create navigation structure with auth and app navigators</name>
  <files>
    src/navigation/types.ts
    src/navigation/index.tsx
    src/navigation/AuthNavigator.tsx
    src/navigation/AppNavigator.tsx
  </files>
  <action>
    Create complete navigation structure for the app:
    
    1. Create src/navigation/types.ts:
       - Define AuthStackParamList with screens: Welcome, Login, Register, ForgotPassword
       - Define AppTabParamList with screens: Home, Expenses, Budget, Settings
       - Define RootStackParamList combining auth and app
       - Export NavigationProp types for type-safe navigation
       
    2. Create src/navigation/AuthNavigator.tsx:
       - Use createNativeStackNavigator from @react-navigation/native-stack
       - Define stack screens: Welcome (initial), Login, Register, ForgotPassword
       - Set headerShown: false for all screens (custom headers later)
       - Apply screen options for transitions
       
    3. Create src/navigation/AppNavigator.tsx:
       - Use createBottomTabNavigator from @react-navigation/bottom-tabs
       - Define tab screens: Home, Expenses, Budget, Settings
       - Set initial route to Home
       - Add placeholder tab icons (MaterialCommunityIcons from react-native-paper)
       - Configure tabBar styling (active/inactive colors)
       
    4. Create src/navigation/index.tsx (root navigation):
       - Import NavigationContainer from @react-navigation/native
       - Import AuthNavigator and AppNavigator
       - Create state isAuthenticated (hardcoded false for now, will connect to real auth in Plan 04)
       - Conditionally render AuthNavigator or AppNavigator based on isAuthenticated
       - Wrap in SafeAreaProvider from react-native-safe-area-context
       - Export as default Navigation component
       
    5. Update App.tsx:
       - Replace content with Navigation component import and render
       - Keep SafeAreaProvider at root level
  </action>
  <verify>
    1. TypeScript compilation: `npm run type-check` passes
    2. Lint check: `npm run lint` passes
    3. Run `npx expo start --web` and verify:
       - Navigation container renders without errors
       - Welcome screen appears (first route in auth stack)
       - No navigation-related console errors
  </verify>
  <done>
    - src/navigation/types.ts exports all navigation param lists
    - src/navigation/AuthNavigator.tsx renders stack with Welcome, Login, Register screens
    - src/navigation/AppNavigator.tsx renders bottom tabs with Home, Expenses, Budget, Settings
    - src/navigation/index.tsx conditionally shows auth or app based on isAuthenticated state
    - App.tsx imports and renders Navigation component
    - No TypeScript errors in navigation files
  </done>
</task>

<task type="auto">
  <name>Task 2: Create auth screen stubs with onboarding flow</name>
  <files>
    src/screens/WelcomeScreen.tsx
    src/screens/LoginScreen.tsx
    src/screens/RegisterScreen.tsx
    src/screens/ForgotPasswordScreen.tsx
    src/screens/HomeScreen.tsx
  </files>
  <action>
    Create all auth and main app screens with basic UI using React Native Paper:
    
    1. Create src/screens/WelcomeScreen.tsx:
       - Use React Native Paper components: Surface, Title, Button
       - Display app name/logo placeholder
       - Two primary actions:
         * "Sign In" button → navigates to Login
         * "Create Account" button → navigates to Register
       - Apply basic styling with Paper theming
       - Onboarding introduction text explaining app value
       
    2. Create src/screens/LoginScreen.tsx:
       - Use Paper components: Surface, Title, TextInput, Button, Text
       - Form fields: email, password (secure text entry)
       - "Sign In" button (disabled for now, will wire in Plan 04)
       - "Forgot Password?" link → ForgotPassword screen (AUTH-04)
       - "Don't have an account? Register" link → Register screen
       - Back button to Welcome screen
       
    3. Create src/screens/RegisterScreen.tsx:
       - Use Paper components: Surface, Title, TextInput, Button
       - Form fields: email, password, confirmPassword (all required)
       - "Create Account" button (disabled, will wire in Plan 04)
       - "Already have an account? Sign In" link → Login screen
       - Back button to Welcome screen
       
    4. Create src/screens/ForgotPasswordScreen.tsx:
       - Use Paper components: Surface, Title, TextInput, Button
       - Form field: email
       - "Send Reset Link" button (disabled, will wire in Plan 04)
       - Back button to Login screen
       - Info text explaining email reset workflow
       
    5. Create src/screens/HomeScreen.tsx:
       - Use Paper components: Surface, Title, Text
       - Display "Home" title
       - Placeholder for future expense list
       - "Logout" button (placeholder, will wire in Plan 04)
       
    6. Create placeholder screens for other tabs:
       - src/screens/ExpensesScreen.tsx (placeholder)
       - src/screens/BudgetScreen.tsx (placeholder)
       - src/screens/SettingsScreen.tsx (placeholder)
  </action>
   <verify>
     1. TypeScript compilation: `npm run type-check` passes
     2. Lint check: `npm run lint` passes
     3. Run `npx expo start --web` and manually verify navigation:
        - Welcome screen shows with app intro and 2 buttons
        - "Sign In" → Login screen with back navigation
        - "Create Account" → Register screen with back navigation
        - All screens have proper Paper styling
   </verify>
  <done>
    - WelcomeScreen.tsx shows app branding, intro text, and 2 action buttons
    - LoginScreen.tsx has email/password inputs and navigation links
    - RegisterScreen.tsx has registration form with validation placeholders
    - ForgotPasswordScreen.tsx has email input for reset workflow
    - HomeScreen.tsx displays main app placeholder
    - All screens use React Native Paper components consistently
    - Navigation between all screens works correctly
    - Back navigation functions properly on all screens
  </done>
</task>

</tasks>

<verification>
Overall Plan 02 verification:
1. Navigation renders WelcomeScreen as initial route
2. User can navigate Welcome → Login → Back ✓
3. User can navigate Welcome → Register → Back ✓
4. All auth screens exist and render
5. Bottom tab navigator defined for main app
6. Navigation types are strongly typed (no implicit any)
7. React Native Paper components used consistently
</verification>

<success_criteria>
- App starts on Welcome screen with "Sign In", "Create Account" buttons and onboarding intro
- Navigation between Welcome ↔ Login ↔ Register works with proper back handling
- All screens use React Native Paper for consistent Material 3 styling
- TypeScript navigation types prevent navigation errors
- Navigation structure supports future auth state integration
- Lint and type-check pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
