---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - .eslintrc.js
  - .prettierrc
  - App.tsx
  - app.json
  - babel.config.js
  - metro.config.js
  - src/types/index.ts
  - src/constants/index.ts
autonomous: true

must_haves:
  truths:
    - "Expo project initializes successfully"
    - "TypeScript compiles without errors"
    - "ESLint and Prettier run without config errors"
    - "App launches to basic screen on emulator/device"
  artifacts:
    - path: "package.json"
      provides: "Dependencies and scripts"
      contains: "expo, typescript, eslint, prettier"
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
      contains: "strict: true, jsx: react-jsx"
    - path: "App.tsx"
      provides: "Root component"
      min_lines: 20
    - path: "src/types/index.ts"
      provides: "Shared type definitions"
      exports: ["RootStackParamList"]
  key_links:
    - from: "tsconfig.json"
      to: "App.tsx"
      via: "TypeScript compilation"
      pattern: "include.*src"
    - from: "package.json"
      to: "node_modules"
      via: "npm install"
      pattern: "expo.*54"
---

<objective>
Set up Expo SDK 54 project with TypeScript, ESLint, Prettier, and basic app shell.

Purpose: Foundation layer that all subsequent plans depend on. Must be solid before building features.
Output: Working Expo project that compiles and runs on emulator/device.
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 1 requirements (AUTH-01 to AUTH-06)
# Technical stack from STATE.md:
# - Expo SDK 54
# - React Native 0.81+
# - TypeScript with strict mode
# - Offline-first architecture requirement
# - App size <50MB constraint
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Expo SDK 54 project with TypeScript template</name>
  <files>
    package.json
    tsconfig.json
    app.json
    App.tsx
    babel.config.js
    metro.config.js
    src/types/index.ts
    src/constants/index.ts
  </files>
  <action>
    Initialize a new Expo project using the blank TypeScript template:
    
    1. Run `npx create-expo-app@latest ExpenseTracker --template blank-typescript`
    2. Configure tsconfig.json with strict TypeScript settings:
       - strict: true
       - jsx: react-jsx
       - moduleResolution: bundler
       - allowSyntheticDefaultImports: true
       - paths alias: "@/*": ["./src/*"]
    3. Create src/ directory structure:
       - src/types/index.ts (shared TypeScript types)
       - src/constants/index.ts (app constants)
       - src/components/ (for future components)
       - src/screens/ (for future screens)
       - src/hooks/ (for future hooks)
       - src/utils/ (for utility functions)
    4. Update App.tsx to be a minimal functional component displaying "ExpenseTracker - Loading..."
    5. Configure app.json with:
       - name: "ExpenseTracker"
       - slug: "expensetracker"
       - version: "1.0.0"
       - orientation: "portrait"
       - backgroundColor: "#ffffff"
    6. Update babel.config.js to support path aliases
    7. Create basic metro.config.js for proper module resolution
    
    Note: Use minimal dependencies to keep app size under 50MB. Avoid unnecessary packages.
  </action>
  <verify>
    Run `npx expo start --web` and verify:
    - No TypeScript compilation errors
    - Metro bundler starts successfully
    - App displays "ExpenseTracker - Loading..." in browser
  </verify>
  <done>
    - Expo project created with TypeScript template
    - tsconfig.json configured with strict mode and path aliases
    - src/ directory structure exists with types/, constants/, components/, screens/, hooks/, utils/
    - App.tsx renders without errors
    - Metro bundler starts successfully
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure ESLint, Prettier, and install core dependencies</name>
  <files>
    .eslintrc.js
    .prettierrc
    package.json
  </files>
  <action>
    Set up code quality tooling and install required dependencies:
    
    1. Install ESLint with React Native configuration:
       - eslint
       - @typescript-eslint/parser
       - @typescript-eslint/eslint-plugin
       - eslint-plugin-react
       - eslint-plugin-react-hooks
       - eslint-plugin-react-native
       - eslint-config-prettier
       
    2. Create .eslintrc.js with:
       - Parser: @typescript-eslint/parser
       - Extends: recommended TypeScript, React, React Hooks, Prettier
       - Rules: no-unused-vars, no-console (warn), @typescript-eslint/explicit-function-return-type (off for React)
       - Settings: react version detect
       
    3. Create .prettierrc with:
       - semi: true
       - singleQuote: true
       - tabWidth: 2
       - trailingComma: es5
       - printWidth: 100
       
    4. Add npm scripts to package.json:
       - "lint": "eslint . --ext .ts,.tsx"
       - "lint:fix": "eslint . --ext .ts,.tsx --fix"
       - "format": "prettier --write ."
       - "type-check": "tsc --noEmit"
       
    5. Install core runtime dependencies needed for Phase 1:
       - @react-navigation/native (v7)
       - @react-navigation/native-stack (v7)
       - @react-navigation/bottom-tabs (v7)
       - react-native-paper (v5)
       - react-native-safe-area-context
       - react-native-screens
       - react-native-gesture-handler
       - expo-sqlite (~14.x)
       - drizzle-orm
       - react-i18next
       - i18next
       - expo-localization
       - expo-secure-store
       - react-native-reanimated (~4.2.x)
       - zod (for validation)
       
    6. Add TypeScript dev dependencies if missing:
       - @types/react
       - @types/react-native
       - typescript
       
    7. Run npm install to install all dependencies
  </action>
  <verify>
    Run the following commands and verify success:
    1. `npm run lint` - should pass with no errors
    2. `npm run type-check` - should pass with no errors
    3. `npx expo start --web` - should start without errors
    4. Check node_modules contains: react-navigation, react-native-paper, drizzle-orm, react-i18next, expo-sqlite
  </verify>
  <done>
    - ESLint configured with .eslintrc.js (no errors on lint run)
    - Prettier configured with .prettierrc
    - npm scripts added: lint, lint:fix, format, type-check
    - All core dependencies installed (check package.json dependencies list)
    - No TypeScript compilation errors
    - App runs successfully with new dependencies
  </done>
</task>

</tasks>

<verification>
Overall Plan 01 verification:
1. Project structure matches expected: src/{types,constants,components,screens,hooks,utils}/
2. TypeScript strict mode enabled and passing
3. ESLint passes with no errors
4. All Phase 1 dependencies installed (package.json check)
5. App launches to "ExpenseTracker - Loading..." screen
</verification>

<success_criteria>
- Expo project initializes with `npx expo start --web` without errors
- TypeScript compiles in strict mode with no errors (`npm run type-check`)
- ESLint passes with no errors (`npm run lint`)
- App displays in browser/emulator with basic "ExpenseTracker - Loading..." text
- Directory structure follows src/ convention with all planned folders
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
