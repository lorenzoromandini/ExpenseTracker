---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - src/i18n/index.ts
  - src/i18n/locales/en/translation.json
  - src/i18n/locales/it/translation.json
  - src/i18n/config.ts
  - src/theme/index.ts
  - src/theme/ThemeProvider.tsx
  - src/hooks/useSecureStorage.ts
  - src/context/AuthContext.tsx
  - src/screens/WelcomeScreen.tsx
  - src/screens/LoginScreen.tsx
  - src/screens/RegisterScreen.tsx
  - App.tsx
  - package.json
autonomous: true

must_haves:
  truths:
    - "App displays in Italian or English based on device locale"
    - "User can toggle between dark and light themes"
    - "Session tokens stored securely with expo-secure-store"
    - "Auth context provides login/logout/register to all screens"
    - "Login persists across app restarts"
  artifacts:
    - path: "src/i18n/index.ts"
      provides: "i18next initialization"
      exports: ["i18n"]
    - path: "src/i18n/locales/en/translation.json"
      provides: "English translations"
      min_lines: 20
    - path: "src/i18n/locales/it/translation.json"
      provides: "Italian translations"
      min_lines: 20
    - path: "src/theme/index.ts"
      provides: "Theme definitions"
      exports: ["lightTheme", "darkTheme"]
    - path: "src/theme/ThemeProvider.tsx"
      provides: "Theme context provider"
      exports: ["ThemeProvider", "useTheme"]
    - path: "src/hooks/useSecureStorage.ts"
      provides: "Secure storage hook"
      exports: ["useSecureStorage"]
    - path: "src/context/AuthContext.tsx"
      provides: "Authentication state management"
      exports: ["AuthProvider", "useAuth"]
  key_links:
    - from: "src/i18n/index.ts"
      to: "translation.json files"
      via: "resources configuration"
      pattern: "resources.*en.*translation"
    - from: "App.tsx"
      to: "src/i18n/index.ts"
      via: "import i18n"
      pattern: "import.*i18n"
    - from: "src/context/AuthContext.tsx"
      to: "src/hooks/useSecureStorage.ts"
      via: "store session token"
      pattern: "useSecureStorage"
    - from: "src/context/AuthContext.tsx"
      to: "src/db/queries/users.ts"
      via: "create user in database"
      pattern: "createUser"
    - from: "src/navigation/index.tsx"
      to: "src/context/AuthContext.tsx"
      via: "useAuth hook"
      pattern: "useAuth"
---

<objective>
Implement i18n (Italian/English), theming (dark/light), secure storage, and auth context to complete Phase 1 foundation.

Purpose: Fulfill AUTH-01 through AUTH-05 requirements with full localization, theming, and secure session management.
Output: Complete foundation layer ready for Phase 2 OCR receipt scanning features.
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Dependencies: Plans 01-01, 01-02, 01-03 must be complete
# Requirements covered: AUTH-01 (register), AUTH-02 (login), AUTH-03 (password reset), AUTH-04 (cloud sync), AUTH-05 (onboarding)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up i18n with Italian/English and theme provider</name>
  <files>
    src/i18n/config.ts
    src/i18n/index.ts
    src/i18n/locales/en/translation.json
    src/i18n/locales/it/translation.json
    src/theme/index.ts
    src/theme/ThemeProvider.tsx
    App.tsx
  </files>
  <action>
    Implement internationalization and theming:
    
    1. Create src/i18n/config.ts:
       - Import i18n from 'i18next'
       - Import { initReactI18next } from 'react-i18next'
       - Import * as Localization from 'expo-localization'
       - Define supported languages: ['en', 'it']
       - Configure i18next with:
         * lng: Localization.locale.split('-')[0] (auto-detect)
         * fallbackLng: 'en'
         * interpolation: { escapeValue: false }
         * react: { useSuspense: false }
       - Export configured i18n instance
       
    2. Create src/i18n/locales/en/translation.json:
       - Add namespaces: common, auth, screens
       - Common: appName, loading, save, cancel, delete
        - Auth: login, register, forgotPassword, email, password, confirmPassword, createAccount, signIn, welcome
       - Screens: home, expenses, budget, settings
       - Add 20+ translation keys covering all auth screens
       
     3. Create src/i18n/locales/it/translation.json:
        - Mirror structure of English
        - Italian translations: "Accedi", "Registrati", "Email", "Password", etc.
        - Use formal Italian (Lei form not needed, use tu)
       
    4. Create src/i18n/index.ts:
       - Re-export i18n from config
       - Export translation helper types
       - Export changeLanguage function wrapper
       
    5. Create src/theme/index.ts:
       - Import { MD3LightTheme, MD3DarkTheme } from 'react-native-paper'
       - Extend light theme with custom colors:
         * primary: '#1976D2'
         * secondary: '#388E3C'
         * background: '#FFFFFF'
         * surface: '#FFFFFF'
         * error: '#D32F2F'
       - Extend dark theme with:
         * primary: '#90CAF9'
         * secondary: '#81C784'
         * background: '#121212'
         * surface: '#1E1E1E'
       - Export lightTheme, darkTheme, Theme type
       
    6. Create src/theme/ThemeProvider.tsx:
       - Import React, createContext, useState, useContext
       - Import { Provider as PaperProvider } from 'react-native-paper'
       - Create ThemeContext with { isDarkMode, toggleTheme, theme }
       - Create ThemeProvider component:
         * State: isDarkMode (default from system preference)
         * Effect: check device color scheme on mount
         * Provide PaperProvider with current theme
         * Export useTheme hook
       
    7. Update App.tsx:
       - Import './src/i18n' (initializes i18n)
       - Wrap Navigation with ThemeProvider
       - Ensure order: ThemeProvider → AuthProvider (in next task) → Navigation
  </action>
  <verify>
    1. TypeScript compilation: `npm run type-check` passes
    2. Lint check: `npm run lint` passes
    3. Run `npx expo start --web` and verify:
       - App starts without i18n errors
       - Console shows detected locale
       - Changing device locale (if possible) or forcing language shows Italian/English
       - Paper components render with proper theming
       - Light/dark mode toggle works (test manually in code)
    4. Check translation files load:
       - Add temporary Text component with {t('auth:login')}
       - Verify "Accedi" shows for Italian locale
  </verify>
  <done>
    - i18n initialized with auto locale detection
    - English translation file has 20+ keys
    - Italian translation file has 20+ keys
    - Theme provider supports light/dark modes
    - PaperProvider wrapped around app with custom theme
    - App.tsx imports i18n initialization
    - No i18n or theming errors in console
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement secure storage and auth context</name>
  <files>
    src/hooks/useSecureStorage.ts
    src/context/AuthContext.tsx
    src/navigation/index.tsx
    src/screens/WelcomeScreen.tsx
    src/screens/LoginScreen.tsx
    src/screens/RegisterScreen.tsx
  </files>
  <action>
    Implement secure session storage and full auth flow:
    
    1. Create src/hooks/useSecureStorage.ts:
       - Import * as SecureStore from 'expo-secure-store'
        - Define STORAGE_KEYS constants:
          * AUTH_TOKEN = 'auth_token'
          * USER_ID = 'user_id'
       - Export async functions:
         * setSecureItem(key: string, value: string): Promise<void>
         * getSecureItem(key: string): Promise<string | null>
         * deleteSecureItem(key: string): Promise<void>
         * clearSecureStorage(): Promise<void>
       - Add error handling for secure store failures
       - Fallback to memory storage if secure store unavailable (for web/dev)
       
    2. Create src/context/AuthContext.tsx:
       - Import React, createContext, useState, useEffect, useContext
        - Import { createUser, getUserByEmail } from '../db/queries/users'
       - Import { getSecureItem, setSecureItem, deleteSecureItem, AUTH_TOKEN, USER_ID } from '../hooks/useSecureStorage'
       - Define AuthUser type: User | null
        - Define AuthContextType:
          * user: AuthUser
          * isLoading: boolean
          * isAuthenticated: boolean
          * login(email, password): Promise<void>
          * register(email, password, displayName): Promise<void>
          * logout(): Promise<void>
          * completeOnboarding(): Promise<void>
        - Implement AuthProvider:
          * State: user, isLoading, isAuthenticated
          * Effect: Check secure storage on mount, restore session
          * login: Verify email/password (compare hash in future), store token
          * register: Create user in DB, store token, set user
          * logout: Clear secure storage, set user null
          * completeOnboarding: Mark onboarding as complete, navigate to app
       - Export useAuth hook
       
    3. Update src/navigation/index.tsx:
       - Import useAuth from '../context/AuthContext'
       - Remove hardcoded isAuthenticated state
       - Use const { user, isLoading, isAuthenticated } = useAuth()
       - Show loading spinner while isLoading
       - Conditionally render AuthNavigator or AppNavigator
       
    4. Update src/screens/WelcomeScreen.tsx:
       - Import { useTranslation } from 'react-i18next'
       - Import { useAuth } from '../context/AuthContext'
       - Use useNavigation for navigation
       - Replace hardcoded text with {t('key')} translations
        - Wire buttons:
          * Sign In → navigation.navigate('Login')
          * Create Account → navigation.navigate('Register')
       - Apply theming from useTheme
       
    5. Update src/screens/LoginScreen.tsx:
       - Use useAuth for login function
       - Use useTranslation for text
       - Form validation: required email, password min 6 chars
       - On submit: await auth.login(email, password)
       - Show loading state during login
       - Error handling: display error message on failure
       - Wire "Forgot Password?" → navigation.navigate('ForgotPassword')
       
    6. Update src/screens/RegisterScreen.tsx:
       - Use useAuth for register function
       - Use useTranslation for text
       - Form validation: valid email, password 8+ chars, passwords match
       - On submit: await auth.register(email, password, displayName)
       - Show loading state
       - Success: auto-navigate to app
       
     7. Update src/screens/HomeScreen.tsx:
        - Add logout button that calls auth.logout()
        - Display user info (email)
        - Show current language
  </action>
  <verify>
    1. TypeScript compilation: `npm run type-check` passes
    2. Lint check: `npm run lint` passes
     3. Run `npx expo start --web` and test full flows:
        
        Test 1 - Registration (AUTH-01):
        - From Welcome → "Create Account"
        - Enter email, password, confirm password
        - Submit → Home screen
        - Verify user email shown
        
        Test 2 - Login (AUTH-02):
        - Logout → back to Welcome
        - "Sign In" → Login screen
        - Enter credentials → Home screen
        
        Test 3 - Session Persistence (AUTH-05):
        - Refresh app → should still be logged in
        - Verify secure storage has token
        
        Test 4 - Onboarding Flow:
        - Clear storage, restart
        - Complete onboarding → Home screen
        
     4. Check translations:
        - Switch device locale to Italian
        - Verify all auth screens show Italian text
  </verify>
  <done>
    - Secure storage functions work for token persistence
    - Registration creates authenticated user with email
    - Login authenticates existing user
    - Session persists across app restarts (secure storage)
    - Logout clears session and returns to Welcome
    - Onboarding completion works
    - All auth screens use translated text
    - Navigation responds to auth state changes
    - No auth-related runtime errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Add password reset UI and integrate translations across all screens</name>
  <files>
    src/screens/ForgotPasswordScreen.tsx
    src/screens/HomeScreen.tsx
    src/screens/SettingsScreen.tsx
    src/i18n/locales/en/translation.json
    src/i18n/locales/it/translation.json
  </files>
  <action>
    Complete password reset flow (UI only, email integration in future phase) and finalize translations:
    
    1. Update src/screens/ForgotPasswordScreen.tsx:
       - Use useTranslation for all text
       - Add form validation for email field
       - Implement handleSendResetLink:
         * Validate email format
         * Show "sending" loading state
         * For now: show success message "Instructions sent" (real email in Phase 6)
         * Store reset token in secure storage for future
       - Add success state UI with "Check your email" message
       - Add "Back to Login" button
       - Apply translations from auth namespace
       
    2. Create placeholder Settings screen with language toggle:
       - Create src/screens/SettingsScreen.tsx:
       - Use useTranslation and i18n.changeLanguage
       - Display current language
       - Add "Language" section with buttons: Italiano, English
       - On press: i18n.changeLanguage('it' | 'en')
       - UI updates immediately with new language
       - Add "Theme" section with Light/Dark toggle using theme context
       
    3. Expand translation files to cover all UI:
       - Add forgotPassword namespace:
         * title: "Reset Password" / "Reimposta Password"
         * instructions: "Enter your email..." / "Inserisci la tua email..."
         * sendButton: "Send Instructions" / "Invia Istruzioni"
         * success: "Check your email" / "Controlla la tua email"
       - Add settings namespace:
         * title: "Settings" / "Impostazioni"
         * language: "Language" / "Lingua"
         * theme: "Theme" / "Tema"
         * light: "Light" / "Chiaro"
         * dark: "Dark" / "Scuro"
         * logout: "Logout" / "Esci"
       - Add validation namespace:
         * required: "Required" / "Richiesto"
         * emailInvalid: "Invalid email" / "Email non valida"
         * passwordShort: "Password must be 8+ characters" / "Password minimo 8 caratteri"
         * passwordsMatch: "Passwords must match" / "Le password devono coincidere"
       
    4. Update HomeScreen with full translations:
       - Use t('screens:home.title') for title
       - Add welcome message with user name
       - Add quick actions placeholder for Phase 2
       
    5. Add password reset placeholder to auth context:
       - Add forgotPassword(email): Promise<void>
       - For now: validate email format, simulate API call with timeout
       - Return success (real implementation needs backend in Phase 6)
       
    6. Final integration check:
       - Ensure all screens import useTranslation
       - Ensure all text uses t() function
       - Verify Italian and English translations are complete
       - Test language switching in Settings
  </action>
  <verify>
    1. TypeScript compilation: `npm run type-check` passes
    2. Lint check: `npm run lint` passes
    3. Run `npx expo start --web` and test:
       
       Test 1 - Password Reset UI (AUTH-04 prep):
       - Login screen → "Forgot Password?"
       - Enter email → "Send Instructions"
       - See success message "Check your email"
       - Can navigate back to Login
       
       Test 2 - Language Toggle:
       - Go to Settings tab
       - Click "Italiano" → all text switches to Italian
       - Click "English" → all text switches to English
       - Restart app → language preference persists
       
       Test 3 - Theme Toggle:
       - Settings → Theme → Dark
       - App switches to dark theme
       - All Paper components use dark colors
       - Switch back to Light → returns to light theme
       
    4. Verify all 6 AUTH requirements:
       - AUTH-01: Guest mode works ✓
       - AUTH-02: Registration works ✓
       - AUTH-03: Login works ✓
       - AUTH-04: Password reset UI ready ✓
       - AUTH-05: Cloud sync can be enabled (placeholder) ✓
       - AUTH-06: Skip onboarding works ✓
  </verify>
  <done>
    - ForgotPasswordScreen complete with translations
    - SettingsScreen has language and theme toggles
    - All translations cover auth, screens, validation, settings namespaces
    - Language switching works and persists
    - Theme switching works (dark/light)
    - All 6 AUTH requirements have working UI
    - No untranslated strings in auth flow
    - No theme inconsistencies
  </done>
</task>

</tasks>

<verification>
Overall Plan 04 verification:
1. App launches and auto-detects device locale
2. All auth screens show translated text (Italian/English)
3. Registration stores user with email in database
4. Login retrieves user and stores session token
5. Session persists after app restart (check secure storage)
6. Password reset UI functional (email flow in future phase)
7. Theme toggle switches between light/dark
8. Language toggle switches between Italian/English
9. Navigation responds correctly to auth state changes
</verification>

<success_criteria>
- i18n initialized with react-i18next and expo-localization
- Italian and English translation files complete (40+ keys each)
- App auto-detects device locale on first launch
- User can manually switch language in Settings (persists)
- Theme provider supports light/dark modes with Material 3
- User can toggle theme in Settings (persists)
- Secure storage stores auth tokens with expo-secure-store
- Auth context provides: login, register, logout
- Session persists across app restarts (verified by reloading)
- All 5 AUTH requirements have working implementations:
  * AUTH-01: Registration with email/password
  * AUTH-02: Login with session persistence
  * AUTH-03: Password reset UI ready
  * AUTH-04: Cloud sync can be enabled (placeholder for Phase 6)
  * AUTH-05: Onboarding completion flow
- No auth, i18n, or theming errors
- App size remains under 50MB (check with expo build)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
