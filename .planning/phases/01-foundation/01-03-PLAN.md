---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - drizzle.config.ts
  - src/db/index.ts
  - src/db/schema.ts
  - src/db/migrations/0000_initial.sql
  - src/db/migrations/meta/_journal.json
  - src/db/queries/users.ts
  - src/types/user.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "SQLite database initializes on app launch"
    - "User data can be stored and retrieved"
    - "Database schema supports authenticated users"
    - "Migrations run automatically on schema changes"
  artifacts:
    - path: "drizzle.config.ts"
      provides: "Drizzle ORM configuration"
      contains: "schema, out, driver"
    - path: "src/db/index.ts"
      provides: "Database connection singleton"
      exports: ["db"]
    - path: "src/db/schema.ts"
      provides: "Drizzle table definitions"
      contains: "users table"
    - path: "src/db/migrations/0000_initial.sql"
      provides: "Initial migration SQL"
      contains: "CREATE TABLE users"
    - path: "src/db/queries/users.ts"
      provides: "User CRUD operations"
      exports: ["createUser", "getUserByEmail", "updateUser"]
    - path: "src/types/user.ts"
      provides: "User TypeScript types"
      exports: ["User", "AuthenticatedUser"]
  key_links:
    - from: "src/db/index.ts"
      to: "src/db/schema.ts"
      via: "import schema"
      pattern: "from.*schema"
    - from: "src/db/queries/users.ts"
      to: "src/db/index.ts"
      via: "import db"
      pattern: "from.*db"
    - from: "src/db/index.ts"
      to: "expo-sqlite"
      via: "openDatabaseSync"
      pattern: "openDatabaseSync"
---

<objective>
Set up SQLite database with Drizzle ORM, create user schema, and implement basic CRUD queries.

Purpose: Offline-first data layer that supports user accounts (AUTH-01 to AUTH-05).
Output: Working database with users table, migrations, and query utilities ready for auth integration.
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Dependencies: Plan 01-01 must be complete (Expo project with dependencies installed)
# Research from STATE.md: Drizzle ORM chosen over WatermelonDB for better Expo integration
# Constraints: Offline-first architecture, GDPR compliance (encryption in future phases)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Drizzle ORM with expo-sqlite and create user schema</name>
  <files>
    drizzle.config.ts
    src/db/index.ts
    src/db/schema.ts
    src/db/migrations/0000_initial.sql
    src/db/migrations/meta/_journal.json
    src/types/user.ts
  </files>
  <action>
    Set up Drizzle ORM with SQLite and define initial user schema:
    
    1. Install additional Drizzle dependencies:
       - drizzle-kit (dev dependency for migrations)
       - @types/better-sqlite3 (if needed for types)
       
    2. Create drizzle.config.ts:
       - schema: './src/db/schema.ts'
       - out: './src/db/migrations'
       - driver: 'expo' (for expo-sqlite)
       - dialect: 'sqlite'
       
     3. Create src/db/schema.ts:
        - Import { sqliteTable, text, integer, real } from 'drizzle-orm/sqlite-core'
        - Define users table with columns:
          * id: text (UUID, primary key)
          * email: text (required)
          * passwordHash: text (required)
          * displayName: text
          * isVerified: integer (1 or 0, boolean)
          * createdAt: integer (timestamp)
          * updatedAt: integer (timestamp)
          * lastSyncedAt: integer (nullable, for future sync)
        - Export users table and User type inference
       
    4. Create src/db/index.ts (database singleton):
       - Import { openDatabaseSync } from 'expo-sqlite'
       - Import { drizzle } from 'drizzle-orm/expo-sqlite'
       - Import * as schema from './schema'
       - Create database instance with name 'expensetracker.db'
       - Export db = drizzle(sqlite, { schema })
       - Export { sqlite } for direct access if needed
       
     5. Create src/types/user.ts:
        - Export interface User with all fields from schema
        - Export type AuthenticatedUser = User & { email: string; passwordHash: string }
        - Export type UserCreateInput = Omit<User, 'id' | 'createdAt' | 'updatedAt'>
       
    6. Generate initial migration:
       - Run `npx drizzle-kit generate` to create 0000_initial.sql
       - Verify migration file created in src/db/migrations/
       - Verify meta/_journal.json created
  </action>
  <verify>
    1. TypeScript compilation: `npm run type-check` passes
    2. Lint check: `npm run lint` passes
    3. Migration files exist:
       - `ls src/db/migrations/*.sql` should show 0000_initial.sql
       - `ls src/db/migrations/meta/_journal.json` should exist
    4. Verify migration SQL contains CREATE TABLE users with all columns
  </verify>
  <done>
    - drizzle.config.ts exists with expo driver configuration
    - src/db/schema.ts exports users table with all required columns
    - src/db/index.ts exports db singleton initialized with expo-sqlite
    - src/types/user.ts exports User, GuestUser, AuthenticatedUser types
    - src/db/migrations/0000_initial.sql exists with CREATE TABLE statement
    - Migration journal file exists at src/db/migrations/meta/_journal.json
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement user queries and database initialization</name>
  <files>
    src/db/queries/users.ts
    src/db/seed.ts
    App.tsx
  </files>
  <action>
    Create user CRUD operations and database initialization logic:
    
     1. Create src/db/queries/users.ts with functions:
        - createUser(input: UserCreateInput): Promise<User>
          * Generate UUID for id
          * Set timestamps (createdAt, updatedAt)
          * Insert into users table
          * Return created user
        - getUserById(id: string): Promise<User | null>
          * Query by primary key
          * Return user or null if not found
        - getUserByEmail(email: string): Promise<User | null>
          * Query by email (for login)
          * Return user or null
        - updateUser(id: string, updates: Partial<User>): Promise<User>
          * Update updatedAt timestamp
          * Apply partial updates
          * Return updated user
        - deleteUser(id: string): Promise<void>
          * Delete user by id
        - listUsers(): Promise<User[]>
          * Return all users (mainly for debugging)
       
     2. Create src/db/seed.ts:
        - Export function initializeDatabase()
        - Check if database is already initialized
        - Run any pending migrations (if needed)
        - Log initialization status
       
    3. Update App.tsx to initialize database:
       - Import initializeDatabase from './src/db/seed'
       - Use useEffect to call initializeDatabase on mount
       - Show loading state while initializing
       - After init, render Navigation component
       - Handle initialization errors gracefully
       
    4. Add error handling:
       - Create src/utils/errors.ts for database error handling
       - Wrap all queries in try/catch
       - Export typed errors: DatabaseError, NotFoundError, etc.
  </action>
  <verify>
    1. TypeScript compilation: `npm run type-check` passes
    2. Lint check: `npm run lint` passes
    3. Run `npx expo start --web` and verify:
       - App initializes without database errors
       - Console shows "Database initialized" or similar message
       - No SQLite-related errors in console
     4. Test queries by temporarily adding test code (remove after):
        - Create a user and log it
        - Retrieve user by ID and verify data matches
  </verify>
  <done>
    - src/db/queries/users.ts exports all CRUD functions
    - createUser() creates authenticated user with email/password
    - getUserByEmail() retrieves user for login
    - updateUser() modifies existing user data
    - App.tsx initializes database on startup
    - No runtime database errors on app launch
    - Console confirms successful database initialization
  </done>
</task>

</tasks>

<verification>
Overall Plan 03 verification:
1. Database initializes without errors on app start
2. Users table exists with correct schema (check via SQLite browser or query)
3. createUser() stores email/password (hashed in future)
4. getUserByEmail() retrieves correct user
5. All TypeScript types match database schema
6. Migrations are tracked and reversible
</verification>

<success_criteria>
- Drizzle ORM configured with expo-sqlite driver
- Users table exists with: id, email, passwordHash, displayName, isVerified, timestamps
- Database initializes automatically on app startup
- Can create authenticated users with email
- Can query users by ID and email
- All database operations typed with TypeScript
- No database-related runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
