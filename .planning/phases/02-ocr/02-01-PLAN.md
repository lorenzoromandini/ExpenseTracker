---
phase: 02-ocr
plan: 01
type: execute
wave: 1
depends_on: ["01-04"]
files_modified:
  - src/screens/ScanReceiptScreen.tsx
  - src/screens/ManualEntryScreen.tsx
  - src/components/CameraView.tsx
  - src/components/QRScanner.tsx
  - src/utils/qrParser.ts
  - src/db/schema.ts
  - src/db/queries/expenses.ts
  - src/navigation/AppNavigator.tsx
autonomous: true

must_haves:
  truths:
    - "User can scan QR codes on Italian receipts"
    - "QR data is parsed and converted to expense"
    - "Manual entry is available as fallback"
    - "Camera permissions are handled gracefully"
  artifacts:
    - path: "src/screens/ScanReceiptScreen.tsx"
      provides: "QR scanning interface"
      min_lines: 80
    - path: "src/utils/qrParser.ts"
      provides: "Italian receipt QR parser"
      exports: ["parseItalianReceiptQR"]
    - path: "src/components/CameraView.tsx"
      provides: "Camera wrapper component"
      exports: ["CameraView"]
    - path: "src/db/queries/expenses.ts"
      provides: "Expense CRUD operations"
      exports: ["createExpense"]
  key_links:
    - from: "ScanReceiptScreen"
      to: "QRScanner"
      via: "onBarcodeScanned callback"
    - from: "QRScanner"
      to: "parseItalianReceiptQR"
      via: "scanned data"
    - from: "parseItalianReceiptQR"
      to: "createExpense"
      via: "expense data object"
---

<objective>
Implement QR code scanning for Italy's Scontrino Elettronico (electronic receipts).

Purpose: Enable instant expense creation from Italian digital receipts via QR code scanning - the primary differentiator for the EU market.
Output: Working QR scanner that parses Italian receipt data and creates expenses automatically.
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/phase-02-ocr.md

# Dependencies: Phase 1 Foundation must be complete
# Requirements covered: OCR-01, OCR-07, OCR-08
# Technical stack:
# - expo-camera: QR/barcode scanning (built into Expo SDK)
# - expo-barcode-scanner: Additional barcode types
# - Works in Expo Go (no dev client needed for this phase)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update database schema for expenses</name>
  <files>
    src/db/schema.ts
    src/db/migrations/0001_add_expenses.sql
  </files>
  <action>
    Add expenses table to support receipt-based expense creation:
    
    1. Update src/db/schema.ts:
       - Import additional types from drizzle-orm/sqlite-core
       - Add expenses table with columns:
         * id: text (UUID, primary key)
         * userId: text (foreign key to users.id)
         * merchant: text (required)
         * amount: real (required)
         * currency: text (default 'EUR')
         * date: integer (timestamp, required)
         * categoryId: text (nullable, for future)
         * notes: text (nullable)
         * receiptImagePath: text (nullable)
         * vatAmount: real (nullable)
         * isManualEntry: integer (boolean, default false)
         * createdAt: integer (timestamp)
         * updatedAt: integer (timestamp)
       - Add index on userId for querying
       
    2. Create migration file src/db/migrations/0001_add_expenses.sql:
       - CREATE TABLE expenses with all columns
       - CREATE INDEX idx_expenses_user_id
       - CREATE INDEX idx_expenses_date (for sorting)
       
    3. Update migration journal
  </action>
  <verify>
    1. TypeScript compilation: `npm run type-check` passes
    2. Schema file has expenses table definition
    3. Migration SQL is valid
  </verify>
  <done>
    - src/db/schema.ts has expenses table
    - Migration 0001_add_expenses.sql created
    - All TypeScript types match
  </done>
</task>

<task type="auto">
  <name>Task 2: Create expense queries</name>
  <files>
    src/db/queries/expenses.ts
    src/types/expense.ts
  </files>
  <action>
    Create expense CRUD operations:
    
    1. Create src/types/expense.ts:
       - Define Expense interface
       - Define ExpenseCreateInput type
       - Define ExpenseUpdateInput type
       
    2. Create src/db/queries/expenses.ts with functions:
       - createExpense(input): Promise<Expense>
         * Generate UUID
         * Set timestamps
         * Insert into database
         * Return created expense
       - getExpenseById(id): Promise<Expense | null>
       - getExpensesByUser(userId): Promise<Expense[]>
         * Sort by date descending
       - updateExpense(id, updates): Promise<Expense>
       - deleteExpense(id): Promise<void>
       - getExpensesByDateRange(userId, start, end): Promise<Expense[]>
    
    3. Add proper error handling
  </action>
  <verify>
    1. TypeScript compilation passes
    2. All CRUD functions exported
    3. Types match database schema
  </verify>
  <done>
    - src/types/expense.ts created with proper types
    - src/db/queries/expenses.ts with all CRUD functions
    - TypeScript types match schema
  </done>
</task>

<task type="auto">
  <name>Task 3: Create QR parser for Italian receipts</name>
  <files>
    src/utils/qrParser.ts
    src/utils/__tests__/qrParser.test.ts
  </files>
  <action>
    Implement Italian Scontrino Elettronico QR code parser:
    
    1. Create src/utils/qrParser.ts:
       - Define QRReceiptData interface:
         * merchantVatNumber: string
         * merchantName: string | null
         * date: Date
         * totalAmount: number
         * receiptNumber: string
         * zNumber: string
         * currency: string
         * rawUrl: string
       
       - Implement parseItalianReceiptQR(qrData: string): QRReceiptData | null
         * Handle URL format: https://... or custom schemes
         * Extract query parameters
         * Parse date from various formats
         * Convert amount string to number
         * Validate required fields
         * Return null if invalid/malformed
       
       - Implement isItalianReceiptQR(qrData: string): boolean
         * Check for Scontrino Elettronico patterns
         * Return true if matches Italian format
       
       - Add JSDoc comments explaining Italian receipt format
    
    2. Create test file with sample QR data:
       - Valid Italian receipt URL
       - Invalid/non-receipt QR
       - Various date formats
       - Edge cases
  </action>
  <verify>
    1. Parser handles known Italian receipt formats
    2. Returns proper data structure
    3. Validates required fields
    4. Tests pass (if test runner configured)
  </verify>
  <done>
    - parseItalianReceiptQR function implemented
    - Handles Italian Scontrino Elettronico format
    - Returns structured data
    - Unit tests created
  </done>
</task>

<task type="auto">
  <name>Task 4: Create camera components</name>
  <files>
    src/components/QRScanner.tsx
    src/components/CameraView.tsx
  </files>
  <action>
    Build camera UI components for QR scanning:
    
    1. Create src/components/QRScanner.tsx:
       - Use expo-camera CameraView
       - Configure for QR code scanning only
       - Show camera preview
       - Overlay: corner brackets (scanning guide)
       - On scan: callback with scanned data
       - Props:
         * onQRScanned: (data: string) => void
         * onError: (error: Error) => void
       
    2. Create src/components/CameraView.tsx:
       - Wrapper with permission handling
       - State: requesting/granted/denied
       - Permission denied UI:
         * Explanation text
         * "Open Settings" button
         * "Enter Manually" button
       - Loading state while initializing camera
       - Theme-aware styling
  </action>
  <verify>
    1. Components render without errors
    2. Camera preview displays
    3. Permission denied state shows fallback UI
    4. QR scanning triggers callback
  </verify>
  <done>
    - QRScanner component with camera preview
    - CameraView wrapper with permissions
    - Proper permission denied fallback
    - Theme-aware styling
  </done>
</task>

<task type="auto">
  <name>Task 5: Create Scan Receipt screen</name>
  <files>
    src/screens/ScanReceiptScreen.tsx
    src/navigation/AppNavigator.tsx
  </files>
  <action>
    Build main scanning interface:
    
    1. Create src/screens/ScanReceiptScreen.tsx:
       - Full-screen camera view
       - Header with back button
       - Instructions overlay:
         * "Align QR code within frame"
         * Icon + text
       - On QR scan:
         * Parse with parseItalianReceiptQR
         * If valid: Show expense preview
         * If invalid: Show "Invalid QR" message, allow retry
       - Bottom buttons:
         * "Flash" toggle (if needed)
         * "Enter Manually" button
       - Loading state while processing
       
    2. Create ExpensePreview sub-component:
       - Show parsed data:
         * Merchant name
         * Amount
         * Date
       - "Save" and "Edit" buttons
       - If merchant unknown: "Add Merchant Name" input
       
    3. Update AppNavigator:
       - Add ScanReceipt screen to tabs or as modal
       - Recommendation: Add FAB button on Home to launch scanner
  </action>
  <verify>
    1. Screen renders with camera
    2. QR scan triggers parsing
    3. Valid QR shows expense preview
    4. Can save expense to database
    5. Manual entry button works
  </verify>
  <done>
    - ScanReceiptScreen with camera UI
    - Expense preview component
    - Integration with database
    - Navigation updated
  </done>
</task>

<task type="auto">
  <name>Task 6: Create Manual Entry screen</name>
  <files>
    src/screens/ManualEntryScreen.tsx
    src/components/AmountInput.tsx
    src/components/DatePicker.tsx
  </files>
  <action>
    Build manual expense entry as fallback:
    
    1. Create src/screens/ManualEntryScreen.tsx:
       - Form fields:
         * Merchant (TextInput)
         * Amount (Number input with currency)
         * Date (Date picker)
         * Category (Dropdown - placeholder)
         * Notes (TextInput, optional)
       - Validation:
         * Required: merchant, amount, date
         * Amount: positive number
         * Date: not in future
       - Buttons:
         * "Save Expense"
         * "Cancel"
       - Currency: Default EUR, show symbol
       
    2. Create reusable components:
       - AmountInput: Currency symbol + number input
       - DatePicker: Native date picker
       - CategoryDropdown: Placeholder for Phase 3
       
    3. Navigation:
       - Accessible from Scan screen
       - Accessible from Home (FAB menu)
  </action>
  <verify>
    1. Form validates required fields
    2. Can save expense to database
    3. Currency formatting correct
    4. Date validation works
  </verify>
  <done>
    - ManualEntryScreen with form
    - Validation working
    - Saves to database
    - Accessible from scan screen
  </done>
</task>

<task type="auto">
  <name>Task 7: Add FAB menu to Home screen</name>
  <files>
    src/screens/HomeScreen.tsx
  </files>
  <action>
    Update Home screen with quick action buttons:
    
    1. Add Floating Action Button (FAB) menu:
       - Primary: Scan Receipt (camera icon)
       - Secondary: Add Manually (plus icon)
       
    2. Use React Native Paper FAB.Group:
       - Expandable menu
       - Icons for each action
       - Labels
       
    3. Navigation:
       - Scan Receipt → ScanReceiptScreen
       - Add Manually → ManualEntryScreen
       
    4. Update Home content:
       - Show empty state if no expenses
       - "Scan your first receipt" CTA
       - Or list recent expenses (Phase 3)
  </action>
  <verify>
    1. FAB appears on Home screen
    2. Expands to show options
    3. Navigates to correct screens
  </verify>
  <done>
    - FAB added to Home
    - Quick actions working
    - Navigation functional
  </done>
</task>

<task type="auto">
  <name>Task 8: Add translations for scanning</name>
  <files>
    src/i18n/locales/en/translation.json
    src/i18n/locales/it/translation.json
  </files>
  <action>
    Add i18n strings for scanning features:
    
    1. English translations:
       - scanReceipt: "Scan Receipt"
       - scanInstructions: "Align QR code within frame"
       - manualEntry: "Add Manually"
       - invalidQR: "Invalid QR code"
       - expensePreview: "Review Expense"
       - saveExpense: "Save Expense"
       - editExpense: "Edit Details"
       - merchant: "Merchant"
       - amount: "Amount"
       - date: "Date"
       - category: "Category"
       - notes: "Notes"
       - required: "Required"
       - cameraPermission: "Camera access lets you scan receipts"
       - openSettings: "Open Settings"
       - tryAgain: "Try Again"
       
    2. Italian translations:
       - All keys translated
       - Formal but friendly tone
  </action>
  <verify>
    1. All UI strings translated
    2. Italian translations natural
    3. TypeScript compilation passes
  </verify>
  <done>
    - All scanning strings translated
    - Italian complete
    - No hardcoded strings
  </done>
</task>

</tasks>

<verification>
Overall Plan 02-01 verification:
1. Database has expenses table with proper schema
2. QR scanner works in Expo Go
3. Italian receipt QR codes are parsed correctly
4. Expenses are saved to database from QR data
5. Manual entry works as fallback
6. Camera permissions handled gracefully
7. All screens translated
8. FAB on Home provides quick access
9. TypeScript compiles without errors
</verification>

<success_criteria>
- User can scan QR codes on Italy's Scontrino Elettronico receipts
- QR data (merchant, amount, date) is extracted and displayed
- User can save expense directly or edit details first
- Manual entry available when camera unavailable
- Camera permissions requested contextually with explanation
- Permission denied shows settings link + manual entry option
- All UI text translated (Italian/English)
- Works in Expo Go (no dev client needed)
- App size remains under 30MB for core features
- Expenses saved to SQLite with proper user association
</success_criteria>

<output>
After completion, create `.planning/phases/02-ocr/02-01-SUMMARY.md`
</output>